model:
  input_dim: 256  # should match paper_encoder proj_dim
  num_labels: 5   # extends, reframes, replaces, strengthens, none
  dropout: 0.1

train:
  lr: 1.0e-4
  batch_size: 64
  epochs: 20
  weight_decay: 0.01
